#include "main.h"
#include <stdlib.h>

/**
 * _abs - return the absolute value of a number
 * @number: input value as an integer
 * Description: prints the absolute value of a number
 * Return: return an integer absolute value
 */
int _abs(int number)
{
	return (abs(number));
}
