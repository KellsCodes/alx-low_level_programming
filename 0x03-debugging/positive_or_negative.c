#include "main.h"

/**
 * positive_or_negative - checks the sign of a number
 * @i: input number
 * Return:void
 */
void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d", 0);
	else if (i == 0)
		printf("%d is zero", 0);
	printf("%d", 1);
	printf("\n");
}
